[project]
name = "dbt-core-demo-cafe"
version = "0.1.0"
description = "dbt project for analyzing cafe operation data"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "dbt-duckdb>=1.8.0",
    "streamlit>=1.32.0",
    "duckdb>=0.10.0",
    "pandas>=2.0.0",
]
